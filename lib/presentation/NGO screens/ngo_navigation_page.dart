import 'package:flutter/material.dart';

class NGONavigationpage extends StatelessWidget {
  const NGONavigationpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}